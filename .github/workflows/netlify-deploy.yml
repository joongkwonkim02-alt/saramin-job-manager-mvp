name: Netlify Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify build hook
        run: |
          curl -sS -X POST -d '{}' "${{ secrets.NETLIFY_BUILD_HOOK_URL }}"
